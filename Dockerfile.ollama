FROM ollama/ollama:latest

ENV OLLAMA_HOST=0.0.0.0:11434
ENV OLLAMA_MODEL=${OLLAMA_MODEL:-llama3}

EXPOSE 11434

# Start Ollama and pull model
CMD ["sh", "-c", "ollama serve & sleep 3 && ollama pull $OLLAMA_MODEL && echo 'Ready!' && sleep infinity"]